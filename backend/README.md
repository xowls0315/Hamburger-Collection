# 햄버거 모음 사이트 - 백엔드

NestJS 기반 REST API 서버

## 🚀 빠른 시작

### 1. 설치

```bash
npm install
```

### 2. 환경 변수 설정

`.env` 파일을 생성하고 필요한 환경 변수를 설정하세요.

```bash
cp .env.example .env
```

자세한 내용은 [INSTALL.md](./INSTALL.md)를 참고하세요.

### 3. 데이터베이스 설정

DBeaver를 사용하여 PostgreSQL 데이터베이스를 설정하세요:

1. DBeaver에서 PostgreSQL 연결
2. `hamburger_collection` 데이터베이스 생성
3. `database/init.sql` 파일 실행

자세한 내용은 [백엔드_작업_가이드.md](./백엔드_작업_가이드.md)의 "1-1. DBeaver를 사용한 PostgreSQL 데이터베이스 설정" 섹션을 참고하세요.

### 4. 실행

```bash
# 개발 모드
npm run start:dev

# 프로덕션 빌드
npm run build
npm run start:prod
```

## 📚 문서

- **[INSTALL.md](./INSTALL.md)**: 설치 및 초기 설정 가이드
- **[백엔드_작업_가이드.md](./백엔드_작업_가이드.md)**: 상세 개발 가이드

## 📁 프로젝트 구조

```
backend/
├── src/                    # 소스 코드
│   ├── auth/              # 인증 모듈
│   ├── users/             # 사용자 모듈
│   ├── brands/            # 브랜드 모듈
│   ├── menu-items/        # 메뉴 아이템 모듈
│   ├── nutrition/         # 영양정보 모듈
│   ├── posts/             # 게시글 모듈
│   ├── comments/          # 댓글 모듈
│   ├── stores/            # 매장 검색 모듈
│   └── admin/             # 관리자 모듈
├── database/              # 데이터베이스 스크립트
│   └── init.sql           # 초기화 SQL
├── test/                  # 테스트 파일
└── .env                   # 환경 변수 (gitignore)
```

## 🛠️ 기술 스택

- **Framework**: NestJS
- **ORM**: TypeORM
- **Database**: PostgreSQL
- **Authentication**: JWT (AccessToken + RefreshToken)
- **OAuth**: Kakao OAuth 2.0

## 📝 주요 기능

- ✅ 카카오 OAuth 로그인
- ✅ JWT 기반 인증 (AccessToken + RefreshToken)
- ✅ 브랜드별 메뉴 조회
- ✅ 영양정보 조회
- ✅ 게시판 (CRUD)
- ✅ 댓글 (CRUD)
- ✅ 카카오 로컬 API를 통한 매장 검색

## 🔧 개발 명령어

```bash
# 개발 서버 실행
npm run start:dev

# 빌드
npm run build

# 프로덕션 실행
npm run start:prod

# 테스트
npm run test

# 린트
npm run lint

# 포맷팅
npm run format
```

## 📄 라이선스

UNLICENSED
