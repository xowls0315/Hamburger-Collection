# 맥도날드 메뉴 이미지 URL 업데이트 가이드

## 📋 기능 설명

맥도날드 공식 메뉴 페이지(https://www.mcdonalds.co.kr/kor/menu/burger)에서 각 버거 메뉴의 이미지 URL을 자동으로 추출하여 데이터베이스의 `image_url` 필드에 업데이트하는 기능입니다.

## 🔧 동작 방식

1. **페이지 스크래핑**: 1~4페이지를 순차적으로 방문
2. **이미지 URL 추출**: 각 메뉴 카드에서 이미지 URL 추출
3. **메뉴 이름 매칭**:
   - 웹사이트의 메뉴 이름에서 "세트", 특수문자(®, ™) 제거
   - 데이터베이스의 메뉴 이름과 정규화하여 매칭
   - 부분 일치 검색으로 유연하게 매칭
4. **데이터베이스 업데이트**: 매칭된 메뉴의 `image_url` 필드 업데이트

## 📡 API 엔드포인트

```
POST /admin/menu-items/mcdonalds/update-images
```

**예시:**

```
POST http://localhost:3001/admin/menu-items/mcdonalds/update-images
Authorization: Bearer {your_access_token}
```

## 📊 응답 형식

```json
{
  "success": true,
  "brand": "맥도날드",
  "total": 20,
  "updated": 18,
  "errors": 2,
  "errorDetails": [
    "메뉴명1: 이미지 URL을 찾을 수 없음",
    "메뉴명2: 페이지 2 처리 실패: ..."
  ]
}
```

## 🔍 메뉴 이름 매칭 로직

### 정규화 과정

1. **"세트" 제거**: "행운버거 골드 스페셜 세트" → "행운버거 골드 스페셜"
2. **특수문자 제거**: "빅맥®" → "빅맥"
3. **공백 정리**: 여러 공백을 하나로 통일
4. **소문자 변환**: 대소문자 구분 없이 비교

### 매칭 우선순위

1. **정확 일치**: 정규화된 이름이 완전히 일치
2. **부분 일치**: 메뉴 이름의 주요 단어들이 모두 포함되는지 확인
3. **포함 검색**: 한쪽이 다른 쪽을 포함하는지 확인

### 예시

- 웹사이트: "행운버거® 골드 스페셜 세트" → 정규화: "행운버거 골드 스페셜"
- 데이터베이스: "행운버거 골드 스페셜" → 정규화: "행운버거 골드 스페셜"
- ✅ **매칭 성공**

- 웹사이트: "빅맥® 세트" → 정규화: "빅맥"
- 데이터베이스: "빅맥" → 정규화: "빅맥"
- ✅ **매칭 성공**

## ⚠️ 주의사항

1. **서버 부하 방지**: 각 페이지마다 0.5초 대기 시간이 있습니다.
2. **웹사이트 구조 변경**: 맥도날드 사이트 구조가 변경되면 선택자를 조정해야 할 수 있습니다.
3. **이미지 URL 형식**: 상대 경로는 자동으로 절대 경로로 변환됩니다.
4. **매칭 실패**: 메뉴 이름이 너무 다르면 매칭에 실패할 수 있습니다.

## 🔍 사용 예시

### Postman 설정

1. **Method**: POST
2. **URL**: `http://localhost:3001/admin/menu-items/mcdonalds/update-images`
3. **Headers**:
   - `Authorization: Bearer {your_access_token}`
   - `Content-Type: application/json`

4. **요청 전송**

### 예상 소요 시간

- 페이지 4개 × 0.5초 대기 + 네트워크 시간: 약 5-10초
- 메뉴 매칭 및 업데이트: 약 2-3초
- **총 예상 시간**: 약 10-15초

## 📝 로그 확인

서버 콘솔에서 다음과 같은 로그를 확인할 수 있습니다:

```
🖼️ 맥도날드 버거 메뉴 20개에 대한 이미지 URL 업데이트 시작...

📄 페이지 1/4 처리 중: https://www.mcdonalds.co.kr/kor/menu/burger?ca=16&page=1
  ✅ 발견: "행운버거® 골드 스페셜 세트" -> 행운버거 골드 스페셜 -> https://...
  ✅ 발견: "빅맥® 세트" -> 빅맥 -> https://...

📄 페이지 2/4 처리 중: ...

📊 총 21개의 메뉴 이미지 URL을 찾았습니다.

  ✅ 업데이트 완료: 행운버거 골드 스페셜 -> https://...
  ✅ 업데이트 완료: 빅맥 -> https://...
  🔗 매칭: "맥스파이시 상하이 버거" <-> "맥스파이시 상하이 버거 세트"

📊 업데이트 완료: 18개 성공, 2개 실패
```

## 🎯 다음 단계

1. Postman으로 요청 전송
2. 서버 로그 확인
3. 데이터베이스에서 업데이트된 이미지 URL 확인:
   ```sql
   SELECT name, image_url
   FROM menu_items
   WHERE brand_id = (SELECT id FROM brands WHERE slug = 'mcdonalds')
     AND category = 'burger';
   ```

## 🔧 문제 해결

### 이미지 URL을 찾을 수 없는 경우

1. **웹사이트 구조 확인**: 맥도날드 사이트의 HTML 구조가 변경되었을 수 있습니다.
2. **선택자 조정**: `admin.service.ts`의 `updateImageUrlsFromMcDonalds` 메서드에서 선택자를 수정해야 할 수 있습니다.
3. **수동 확인**: 특정 메뉴의 이미지 URL을 수동으로 확인하고 직접 업데이트할 수 있습니다.

### 매칭 실패

1. **메뉴 이름 확인**: 데이터베이스의 메뉴 이름과 웹사이트의 메뉴 이름이 너무 다를 수 있습니다.
2. **정규화 로직 조정**: `normalizeMenuName` 함수를 수정하여 더 유연한 매칭이 가능하도록 할 수 있습니다.
